#!/bin/bash
#
# Author: Christophe Benz <cbenz@easter-eggs.com>
#
# Launch editor on files matching git grep pattern.


set -e

PATTERN="$1"
if [ -z "$PATTERN" ]; then
    echo "usage: git grep-edit <pattern>"
    exit 1
fi

git grep -l "$PATTERN" | xargs -I "{}" xdg-open "{}"
